export const selectGalleryQuery = (state) => state.gallery.query;
export const selectGalleryItems = (state) => state.gallery.items;
export const selectGalleryFiltered = (state) => state.gallery.filtered;
